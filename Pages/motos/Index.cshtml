@page
@model proiect.Pages.motos.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Moto</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.moto[0].marca)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.moto[0].model)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.moto[0].pret)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.moto[0].PublishingDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.moto[0].Dealer.DealerName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.moto) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.marca)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.model)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.pret)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PublishingDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Dealer.DealerName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
